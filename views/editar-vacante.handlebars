<main class="contenedor">
    <form action="/vacantes/editar/{{vacante.url}}" method="POST" class="default-form">
        <h3>Informacion General</h3>

        <div class="campo">
            <label for="">Titulo</label>
            <input type="text" name="titulo" placeholder="Ej React Developer" value="{{vacante.titulo}}" required>
        </div>

        <div class="campo">
            <label for="">Empresa</label>
            <input type="text" name="empresa" placeholder="Ej Empresa contrata" value="{{vacante.empresa}}" required>
        </div>

        <div class="campo">
            <label for="">Ubicacion</label>
            <input type="text" name="ubicacion" placeholder="Ej Santiago o Remoto" value="{{vacante.ubicacion}}" required>
        </div>

        <div class="campo">
            <label for="">Salario (USD)</label>
            <input type="text" name="salario" placeholder="Ej $500 USD" value="{{vacante.salario}}" required>
        </div>

        <div class="campo">
            <label for="">Contrato</label>
            <select name="contrato">
                {{#tipoContrato vacante.contrato}}
                    <option value="" disabled selected>-- Selecciona --</option>
                    <option value="freelance">Feeelance</option>
                    <option value="Tiempo Completo">Tiempo Completo</option>
                    <option value="Medio Tiempo">Medio Tiempo</option>
                    <option value="Por Proyecto">Por Proyecto</option>
                {{/tipoContrato}}
            </select>
        </div>

        <h3>Descripcion del puesto</h3>
        <div class="campo descripcion">
            <label for="">Descripcion</label>
            <input type="hidden" id="x" value="{{{vacante.descripcion}}}" name="descripcion">
            <trix-editor input="x"></trix-editor>
        </div>

        <h3>Conocimientos</h3>

        <lu class="lista-conocimientos">
            {{#seleccionarSkills vacante.skills}}
            {{/seleccionarSkills}}
        </lu>

        <div class="campo centrar-horizontal">
            <input type="hidden" name="skills" id="skills">
            <input type="submit" value="Guardar Cambios" class="btn btn-azul">
        </div>
    
    </form>
</main>