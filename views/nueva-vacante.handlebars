<main class="contenedor">
    <form action="/vacantes/nueva" method="POST" class="default-form">
        <h3>Informacion General</h3>

        <div class="campo">
            <label for="">Titulo</label>
            <input type="text" name="titulo" placeholder="Ej React Developer" >
        </div>

        <div class="campo">
            <label for="">Empresa</label>
            <input type="text" name="empresa" placeholder="Ej Empresa contrata" >
        </div>

        <div class="campo">
            <label for="">Ubicacion</label>
            <input type="text" name="ubicacion" placeholder="Ej Santiago o Remoto" >
        </div>

        <div class="campo">
            <label for="">Salario (USD)</label>
            <input type="text" name="salario" placeholder="Ej $500 USD" >
        </div>

        <div class="campo">
            <label for="">Contrato</label>
            <select name="contrato">
                <option value="" disabled selected>-- Selecciona --</option>
                <option value="freelance">Feeelance</option>
                <option value="Tiempo Completo">Tiempo Completo</option>
                <option value="Medio Tiempo">Medio Tiempo</option>
                <option value="Por Proyecto">Por Proyecto</option>
            </select>
        </div>

        <h3>Descripcion del puesto</h3>
        <div class="campo descripcion">
            <label for="">Descripcion</label>
            <input type="hidden" id="x" name="descripcion">
            <trix-editor input="x"></trix-editor>
        </div>

        <h3>Conocimientos</h3>

        <lu class="lista-conocimientos">
            {{#seleccionarSkills a}}
            {{/seleccionarSkills}}
        </lu>

        <div class="campo centrar-horizontal">
            <input type="hidden" name="skills" id="skills">
            <input type="submit" value="publicar" class="btn btn-azul">
        </div>
    
    </form>
</main>